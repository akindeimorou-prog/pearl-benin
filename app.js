function card(p){return `<article class="product-card"><a href="produit.html?id=${p.id}"><div class="product-image"><img src="${p.image}" alt="${p.name}"></div></a><div class="product-info"><div class="product-meta">${p.category}</div><h3>${p.name}</h3><div class="price">${formatPrice(p.price)}</div><div class="card-actions"><a class="btn outline" href="produit.html?id=${p.id}">Voir</a><button class="btn" data-add="${p.id}">Ajouter</button></div></div></article>`}
function bindAdds(scope=document){scope.querySelectorAll("[data-add]").forEach(b=>b.onclick=()=>addToCart(b.dataset.add))}
function initShop(){const g=document.querySelector("[data-products]");if(!g)return;let cat="Tous";function draw(){g.innerHTML=(cat==="Tous"?PEARL_PRODUCTS:PEARL_PRODUCTS.filter(p=>p.category===cat)).map(card).join("");bindAdds(g)}document.querySelectorAll("[data-filter]").forEach(b=>b.onclick=()=>{document.querySelectorAll("[data-filter]").forEach(x=>x.classList.remove("active"));b.classList.add("active");cat=b.dataset.filter;draw()});draw()}
function initHome(){const g=document.querySelector("[data-featured]");if(g){g.innerHTML=PEARL_PRODUCTS.slice(0,4).map(card).join("");bindAdds(g)}}
function initProduct(){const b=document.querySelector("[data-product-detail]");if(!b)return;const p=getProduct(new URLSearchParams(location.search).get("id"))||PEARL_PRODUCTS[0];b.innerHTML=`<div class="product-main-image"><img src="${p.image}" alt="${p.name}"></div><div class="product-detail"><div class="product-meta">${p.category}</div><h1>${p.name}</h1><div class="price">${formatPrice(p.price)}</div><p class="description">${p.description}</p><div class="option-row"><label>Taille</label><div class="options" id="sizes">${p.sizes.map((x,i)=>`<button class="option ${i?"":"selected"}" data-v="${x}">${x}</button>`).join("")}</div></div><div class="option-row"><label>Couleur</label><div class="options" id="colors">${p.colors.map((x,i)=>`<button class="option ${i?"":"selected"}" data-v="${x}">${x}</button>`).join("")}</div></div><div class="option-row"><label>Quantité</label><div class="quantity"><button id="minus">−</button><span id="qty">1</span><button id="plus">+</button></div></div><button class="btn gold" id="add" style="width:100%">Ajouter au panier</button><div class="notice">Disponibilité et livraison à confirmer lors de la commande.</div></div>`;let size=p.sizes[0],color=p.colors[0],q=1;document.querySelectorAll("#sizes .option").forEach(x=>x.onclick=()=>{document.querySelectorAll("#sizes .option").forEach(y=>y.classList.remove("selected"));x.classList.add("selected");size=x.dataset.v});document.querySelectorAll("#colors .option").forEach(x=>x.onclick=()=>{document.querySelectorAll("#colors .option").forEach(y=>y.classList.remove("selected"));x.classList.add("selected");color=x.dataset.v});document.getElementById("minus").onclick=()=>{q=Math.max(1,q-1);document.getElementById("qty").textContent=q};document.getElementById("plus").onclick=()=>{q++;document.getElementById("qty").textContent=q};document.getElementById("add").onclick=()=>addToCart(p.id,q,size,color)}
document.addEventListener("DOMContentLoaded",()=>{updateCartCount();initShop();initHome();initProduct();document.querySelectorAll("[data-menu]").forEach(b=>b.onclick=()=>{const n=document.querySelector(".navlinks");n.style.display=n.style.display==="flex"?"none":"flex"})});
